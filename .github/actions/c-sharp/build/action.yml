name: "Build"
description: "Executa o step de Build"
runs:
  using: composite
  steps:
    - name: Run Build App
      shell: bash
      run: |
        echo "Build App"
        echo ${{ inputs.REGISTY_NAME_FAKE }}
        echo ${{ inputs.NAME_PROJECT_FAKE }}
        echo $LOCATION_DOCKERFILE_DOCKER_COMPOSE_FAKE
        echo ${{ inputs.PROJECT_FAKE }}
